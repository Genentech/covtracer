linters:
    linters_with_defaults(
		defaults = default_linters,
	    object_length_linter = NULL,
	    object_name_linter = NULL,
	    cyclocomp_linter = NULL
	)
exclusions:
    list(
        "tests/testthat/packages",
        "inst/examplepkg",
        "vignettes"
    )
