# Parse the expression associated with a srcref

Parse the expression associated with a srcref

## Usage

``` r
srcref_expr(ref)
```

## Arguments

- ref:

  a `srcref`

## Value

A parsed `srcref` object
