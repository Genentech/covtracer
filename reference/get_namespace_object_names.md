# Get namespace exports, filtering methods tables and definitions

Get namespace exports, filtering methods tables and definitions

## Usage

``` r
get_namespace_object_names(ns)
```

## Arguments

- ns:

  A namespace object

## Value

The names of exported objects, filtering internal method tables and
metadata.
