# Convert an expression, call or symbol to a single-line string

Convert an expression, call or symbol to a single-line string

## Usage

``` r
expr_str(ref)
```

## Arguments

- ref:

  a `srcref`

## Value

The given expression, formatted as a string with prefixes for symbols
and generics.
