# Get the full path to the srcref file

Get the full path to the srcref file

## Usage

``` r
getSrcFilepath(x)
```

## Arguments

- x:

  A `srcref` or `list_of_srcref` object

## Value

A `character` vector of source file paths.
